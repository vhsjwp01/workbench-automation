
export PROJECT=`pwd`
export TRAVAIL=${PROJECT}/Logs
export LOGS=${TRAVAIL}
export SOURCE=${PROJECT}/source
export PARAM=${PROJECT}/param
export TMPPROJECT=${PROJECT}/tmp

export REFINEDIR=/product/art_wb12110/refine
export PHOENIX=${REFINEDIR}

if [ `uname -m` = "x86_64" ]
then
	REFINEDISTRIB=Linux64
else
	REFINEDISTRIB=Linux32		
fi
export REFINEDISTRIB

export FREE_FORMAT=N
